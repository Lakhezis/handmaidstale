/* Lightbox2: grupos definidos por data-lightbox en personajes.html. */
lightbox.option({
    albumLabel: 'Imagen %1 de %2',
    alwaysShowNavOnTouchDevices: true,
    disableScrolling: true,
    wrapAround: true,
    fadeDuration: 200,
    imageFadeDuration: 200
});
